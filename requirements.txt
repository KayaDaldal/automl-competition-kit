# Core dependencies — enough for the three tabular engines
# (classification, regression, forecast).
#
# The image engine needs requirements-image.txt on top of this. It is kept
# separate on purpose: a tabular user should not have to install torch.
#
# Last verified with: numpy 2.4.4, pandas 3.0.2, scikit-learn 1.8.0,
# lightgbm 4.7.0, xgboost 3.2.0, catboost 1.2.10, pyyaml 6.0.3.

numpy>=1.24,<3
pandas>=2.0,<4
scikit-learn>=1.3,<2
lightgbm>=4.0,<5
xgboost>=2.0,<4
catboost>=1.2,<2
pyyaml>=6.0,<7
