# Extra dependencies for the image engine only. Install on top of
# requirements.txt:
#
#   pip install -r requirements.txt -r requirements-image.txt
#
# torch here is whatever wheel pip picks for your platform. For a specific
# CUDA build, install torch yourself first from pytorch.org and then run this
# file — pip will leave the existing torch alone.
#
# Last verified with: torch 2.14.0, timm 1.0.29, albumentations 2.0.8,
# opencv-python-headless 4.13.0, pillow 12.2.0.

torch>=2.0,<3
timm>=0.9,<2
albumentations>=1.3,<3
opencv-python-headless>=4.7,<5
pillow>=9.0,<13
